"""研究「既存モデルの改善」: 既存の4つの予想（全頭の3着以内・穴馬の3着以内・人気馬の4着以下・レースの荒れ具合）を、
計画（``plans/既存モデルの修正計画.md``）に沿って直し、直す前と直した後とオッズだけの基準を、同じ期間・同じ時点で比べる。
そのあと、直したモデルを組み合わせた買い方を、過去のレースで確かめる。

入口は ``build_tables.py``（学習データを全期間ぶん作って保存する）、``walk_forward.py``（期間ごとに学習して予測を残す）、
``compare.py``（比べる表を出す）、``backtest.py``（組み合わせた買い方を確かめる）。部品は ``analysis/``。
"""
