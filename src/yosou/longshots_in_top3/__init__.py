"""穴馬が3着以内に入るかを予想する（設計書 ``docs/design/穴馬が3着以内に入るかを予想/``）。

レースに出る穴馬（13頭以下なら 4番人気以下、14頭以上なら 6番人気以下。人気馬の裏返し）1頭ずつについて、
「3着以内に入る確率」を、LightGBM と CatBoost の予測確率の平均で出す。
木曜・前日・当日の3つの時点ごとに、その時点で分かる特徴量だけで学習した別のモデルを使う。
中穴・大穴の区分は、出力を絞るときにだけ使い（``predict --zone``）、モデルには渡さない。

データを読む・特徴量を作る・学習する・予測するクラスは、ほかの予想と共通で ``yosou.shared``（設計書 04）。
予測のときの人気を決める部品・締め切り前のオッズ・まとまり J（人気と人気の履歴）・目的変数「3着以内」も
共通なので ``yosou.shared`` にある。ここには、この予想だけの決めごとと、それを渡してほかを呼ぶところを置く。

| フォルダ | 中身 |
|---|---|
| ``command/`` | コマンド（train・predict）の引数。入口 |
| ``workflow/`` | 予測の流れ（ほかを順に呼ぶだけ）と、予測を出す時点 |
| ``dataset/`` | 穴馬の決まり・区分・行の選び方・区分での絞り込みと、共通の ``DatasetBuilder`` の組み立て |
| ``feature/`` | この予想の特徴量の一覧（``CATALOG``。共通の A〜I に、共通の J を足したもの） |
| ``setting/`` | ハイパーパラメータの初期値のファイル |
| ``tests/`` | この予想の組み立てのテスト（共通の部品のテストは ``yosou.shared.tests``） |

決まり: 1ファイルに1クラス。1クラスに1つの仕事。SQL は ``yosou.shared.repository`` だけ。
参照の向きは上から下へ一方向（``command`` → ``workflow`` → ``dataset`` → ``feature``）。
``yosou.shared`` は参照してよいが、``yosou.shared`` からここを参照しない。ほかの予想のパッケージも参照しない。
"""
