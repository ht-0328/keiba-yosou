"""1番人気を買うか消すかを予想する（設計書 ``docs/design/1番人気を買うか消すかを予想/``）。

1番人気だけを、勝利（1着）・馬券内（1〜3着。勝った馬も含む）・馬券外（4着以下）の3つのグループに分け、
芝ダート × 距離の単位ごとに、グループごとのモデルをそのグループの馬だけで作る（k近傍法）。
対象の1番人気が各モデルにどれだけ近いかを 0〜100 の点数で出し、
馬券外に近ければ「消す」、消さなかった馬は勝利に近ければ「単勝と複勝」、そうでなければ「複勝だけ」と判定する。
LightGBM と CatBoost は、1つのグループだけのデータでは学習できないので使わない。

方針（単位のまとめ方・特徴量の時点・k・判定の線・掛け金・評価の年）は ``setting/default_settings.toml`` にまとめ、
``--config`` で変えて学習し直せるようにしてある。

| フォルダ | 中身 |
|---|---|
| ``command/`` | コマンド（train・evaluate・predict）の引数。入口 |
| ``workflow/`` | 学習・1年ごとの評価・予測の流れ（ほかを順に呼ぶだけ） |
| ``evaluation/`` | 掛け金と、年ごと・判定ごと・単位ごとの成績の表 |
| ``decision/`` | 3つの点数から判定する |
| ``similarity/`` | 近さのモデル（特徴量の変換・グループごとの近さ・単位ごとの一式） |
| ``unit/`` | 芝ダート × 距離の単位の決め方 |
| ``dataset/`` | 1番人気の行の選び方・グループの列と、共通の ``DatasetBuilder`` の組み立て |
| ``feature/`` | この予想の特徴量の一覧（``CATALOG``） |
| ``setting/`` | 方針の初期値と、それを読むクラス |
| ``repository/`` | 学習したモデルの一式の読み書き |
| ``tests/`` | この予想のテスト |

決まり: 1ファイルに1クラス。1クラスに1つの仕事。SQL は ``yosou.shared.repository`` だけ。
参照の向きは上から下へ一方向（``command`` → ``workflow`` → ``evaluation``・``decision`` → ``similarity`` → ``unit``・``dataset`` → ``feature``）。
``yosou.shared`` は参照してよいが、``yosou.shared`` からここを参照しない。ほかの予想のパッケージも参照しない。
"""
