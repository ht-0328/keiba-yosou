"""レースの荒れ具合を4段階で予想する（設計書 ``docs/design/レースの荒れ具合を4段階で予想/``）。

1レースごとに、券種（単勝・馬連・3連複・3連単）ごとの荒れ具合（固い・中荒れ・大荒れ・超荒れ）の確率を、
多クラス分類の LightGBM と CatBoost の確率の平均で出す。木曜・前日・当日の3つの時点ごとに、その時点で分かる
特徴量だけで学習した別のモデルを使う。学習データは 1行 = 1レースで、1頭ごとの特徴量をレース単位に集約して作る。

データを読む・1頭ごとの特徴量を作る・レース単位に集約する・学習する・予測するクラスは、ほかの予想と共通で
``yosou.shared``（設計書 04）。ここには、この予想だけの決めごとと、それを渡してほかを呼ぶところを置く。

| フォルダ | 中身 |
|---|---|
| ``command/`` | コマンド（train・predict）の引数。入口 |
| ``workflow/`` | 予測の流れ（ほかを順に呼ぶだけ）、予測を出す時点、券種ごとのモデルの置き場所 |
| ``dataset/`` | レースを選ぶ・券種・荒れ具合・線引き・目的変数を付ける、共通の ``RaceDatasetBuilder`` の組み立て |
| ``feature/`` | まとまり A〜E を集約するクラスと、この予想の特徴量 49個の一覧（``CATALOG``） |
| ``evaluation/`` | 利用者の規則（1番人気 4.0倍以上かつ 2〜5番人気 10倍未満）の基準 |
| ``setting/`` | ハイパーパラメータの初期値のファイル |
| ``tests/`` | この予想の組み立てのテスト（共通の部品のテストは ``yosou.shared.tests``） |

決まり: 1ファイルに1クラス。1クラスに1つの仕事。SQL は ``yosou.shared.repository`` だけ。
参照の向きは上から下へ一方向（``command`` → ``workflow`` → ``evaluation`` → ``dataset`` → ``feature``）。
``yosou.shared`` は参照してよいが、``yosou.shared`` からここを参照しない。
"""
