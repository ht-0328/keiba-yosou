"""人気馬が4着以下になるかを予想する（設計書 ``docs/design/人気馬が4着以下になるかを予想/``）。

レースに出る人気馬（13頭以下なら 1〜3番人気、14頭以上なら 1〜5番人気）1頭ずつについて、
「4着以下になる確率」を、LightGBM と CatBoost の予測確率の平均で出す。
前日・当日の2つの時点ごとに、その時点で分かる特徴量だけで学習した別のモデルを使う。

データを読む・特徴量を作る・学習する・予測するクラスは、ほかの予想と共通で ``yosou.shared``（設計書 04）。
ここには、この予想だけの決めごとと、それを渡してほかを呼ぶところを置く。

| フォルダ | 中身 |
|---|---|
| ``command/`` | コマンド（train・predict）の引数。入口 |
| ``workflow/`` | 予測の流れ（ほかを順に呼ぶだけ）と、予測を出す時点 |
| ``dataset/`` | 人気馬の行の選び方・目的変数（4着以下）・人気の決め方と、共通の ``DatasetBuilder`` の組み立て |
| ``feature/`` | この予想で足すまとまり J（人気と人気の履歴）と、特徴量の一覧（``CATALOG``） |
| ``repository/`` | 締め切り前の単勝オッズを読む |
| ``setting/`` | ハイパーパラメータの初期値のファイル |
| ``tests/`` | この予想の組み立てのテスト（共通の部品のテストは ``yosou.shared.tests``） |

決まり: 1ファイルに1クラス。1クラスに1つの仕事。SQL は ``repository/`` だけ。
参照の向きは上から下へ一方向（``command`` → ``workflow`` → ``dataset`` → ``feature``。``dataset`` は
``repository`` を使う）。``yosou.shared`` は参照してよいが、``yosou.shared`` からここを参照しない。
"""
