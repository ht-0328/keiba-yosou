# tools/ — 元DB を取得・検索・集計する道具

**何をするものか。** jvdata-store が貯めた DuckDB（元DB）から、レース・馬・出走・事象（人気馬が負けた／穴馬が勝った）・成績を取り出す道具の集まり。コマンド（CLI）と検索画面（ブラウザ）の両方から、同じ条件で同じ結果が得られる。

**誰が使うか。** 利用者と Claude。Claude は DB の中身や列名を調べて SQL を組み立てる前に、まず「早見表」の道具で済ませる。

**使わないこと。** 取得・取り込み（jvdata-store の責務）。予想の理論づくり（`research/`）。元DB は読むだけ。

## 言葉

| 言葉 | 意味 | どこにある |
|---|---|---|
| 元DB | jvdata-store が作った DuckDB。1レコード種別 = 1表（`ra` `se` `hr` …）。列名は JV-Data 仕様書の日本語のまま、値は全部文字列 | `../jvdata-store/jvdata.duckdb` |
| rid | レースの鍵6列（開催年・開催月日・競馬場コード・開催回・開催日目・レース番号）を連結した16桁 | 例 `2026090606040211` |
| hid | 馬の鍵 = 血統登録番号（10桁） | 例 `2021100001` |
| 事実表 | 1行 = 1頭の出走。レース・馬・市場・結果・払戻・前走を型付きの列に並べた一時表。検索・集計・事象の土台 | `tools/共通/facts.py`（列の一覧は `出走検索/runners.py --columns`） |
| 成績7つ | 着別度数（`1着-2着-3着-着外`）・勝率・連対率・複勝率・馬券外率・単勝回収率・複勝回収率 | `tools/共通/perf.py` |
| 絞り込み | 競馬場・芝ダ・コース・距離・馬場・クラス・人気・オッズ・枠・性別・馬齢・頭数・騎手・調教師・期間・月。CLI のフラグ名 = 画面の URL パラメータ名 | `tools/共通/filters.py` |
| 出馬表 | これから走る1レースの出走馬の表。レース前に分かる材料（斤量・騎手・近走 …）だけを並べ、結果は載せない。木曜に出る「出走馬名表」は枠番・馬番が未定、金・土に出る「出馬表」で決まる（`ra`・`se` のデータ区分 1・2） | `tools/共通/card.py`（例: 2026-09-19 中山 11R = rid `2026091906040511`） |
| 事象 | 「人気馬が負けた」「穴馬が勝った」のように、条件に当てはまる出走 | `tools/共通/events.py` |
| 傾向スコア | 当日のレースと条件が同じ過去レースの傾向から、出走馬を項目ごとに +1 / −1 で採点して付けた順位。項目は 164 個（プラス 89・マイナス 75） | `tools/共通/trend.py`、項目の一覧は `tools/傾向スコア/score-items.md` |
| 母集団の段 | 傾向を出す過去レースの広さ。狭い順に 同レース → 4つ一致（競馬場・コース・距離・馬場状態）→ 3つ以上一致 → 2つ以上一致。開催日より前・同じ芝ダだけ | `tools/共通/trend.py` の `LEVELS` |
| 推定脚質 | 今回より前の直近3走の脚質の中央。脚質はレース後にしか分からないので、レース前の材料としてはこれを使う（事実表の `style_before`） | `tools/共通/facts.py` |
| 切り口 | 成績を分ける軸（単勝人気・オッズ帯・騎手・コース単位 …） | `成績集計/perf.py --list` |
| 合成DB | テスト用の小さな DuckDB。値は架空。実DB が無くても道具を試せる | `tools/合成DB/synth.py` |
| 共通 | ツール同士で使い回す部品。入口を持たない | `tools/共通/` |

## 早見表: やりたいこと → コマンド

リポジトリ直下（`keiba-yosou/`）で実行する。`uv` が PATH に無いときは下の「実行の準備」を見る。

| やりたいこと | コマンド |
|---|---|
| DB に何がどこまで入っているか | `uv run python tools/DBの状態/db_info.py` |
| 表の一覧 | `uv run python tools/テーブル閲覧/table.py --list` |
| 表の列の一覧 | `uv run python tools/テーブル閲覧/table.py se --describe` |
| 表の中身を開催日と 列=値 で絞って見る | `uv run python tools/テーブル閲覧/table.py se --from 2026-09-06 --to 2026-09-06 --where 競馬場コード=06 --where レース番号=11` |
| SQL を1文流す | `uv run python tools/任意SQL/sql.py "select count(*) from ra"` |
| 今週（今日以降）のレースの一覧 | `uv run python tools/今週の出馬表/card.py`（日や場で絞るなら `--date 2026-09-19 --venue 中山`） |
| これから走る1レースの出馬表と各馬の近走 | `uv run python tools/今週の出馬表/card.py --date 2026-09-19 --venue 中山 --race 11`（または rid。`--runs 3` で近走の数） |
| その日その場の全レースの出馬表をファイルに | `uv run python tools/今週の出馬表/card.py --date 2026-09-19 --venue 中山 --all --out reports/cards/0919-中山.md` |
| ある週末のレース一覧（確定成績） | `uv run python tools/レース一覧/races.py --from 2026-09-06 --to 2026-09-07` |
| 1レースの出走表・結果・ラップ・通過順・払戻 | `uv run python tools/レース詳細/race.py --date 2026-09-06 --venue 中山 --race 11`（または rid） |
| 馬の過去走（前走の材料） | `uv run python tools/馬の過去走/horse.py <hid> --before 2026-09-06` |
| 条件を組み合わせて出走を検索 | `uv run python tools/出走検索/runners.py --venue 東京 --pop 1 --from 2026-01-01` |
| 着順で絞る（例: 1番人気が馬券外） | `uv run python tools/出走検索/runners.py --pop 1 --finish 4- --from 2026-01-01` |
| 人気馬が負けた出走を集める | `uv run python tools/人気馬が負けたレース/lost_favorites.py --from 2025-01-01` |
| 穴馬が勝った出走を集める | `uv run python tools/穴馬が勝ったレース/longshot_wins.py --from 2025-01-01` |
| 条件に合う出走全体の成績7つを1行で出す | `uv run python tools/成績集計/perf.py total --venue 東京 --pop 1 --odds -1.9` |
| 成績7つを切り口ごとに出す | `uv run python tools/成績集計/perf.py popularity --venue 東京 --course 芝・左 --distance 1600 --condition 良` |
| 集計が正しいか答え合わせ | `uv run python tools/成績集計/perf.py --check` |
| 条件の中で回収率 100% 超えの切り口の値を探す | `uv run python tools/回収率探索/explore.py --venue 東京 --course 芝・左 --distance 1600 --condition 良` |
| 当日のレースの傾向を出して、出走馬を採点・順位付けする（グラフ付きのページ） | `uv run python tools/傾向スコア/trend_score.py --date 2026-09-19 --venue 中山 --race 11 --condition 良 --pops "3:1,7:2" --html --open` |
| その日の全レースを採点して、一覧のページと各レースのページを書く | `uv run python tools/傾向スコア/trend_score.py --date 2026-09-19 --all --open`（`--venue 中山` で競馬場を絞る。`--condition 良` も付けられる） |
| 同じ採点を表で見る（終わったレースなら着順も並ぶ） | `uv run python tools/傾向スコア/trend_score.py --date 2026-09-13 --venue 中山 --race 11 --detail` |
| 点数化項目の一覧 | `uv run python tools/傾向スコア/trend_score.py --items`（説明は `tools/傾向スコア/score-items.md`） |
| 傾向スコアが当たっているかを終わったレースで確かめる | `uv run python tools/傾向スコア/backtest.py --from 2026-08-01 --to 2026-08-31 --out reports/trend-score/backtest-2026-08.md` |
| 検索画面を開く | `uv run python tools/検索画面/web.py --open`（ダブルクリックなら `tools/検索画面/run.bat`） |
| 実DB なしで試す | `uv run python tools/合成DB/synth.py --out reports/synth.duckdb` → 各ツールに `--db reports/synth.duckdb`（出馬表は `--from 2025-04-19` も付ける。合成DB の確定前のレースがその日） |
| テストを走らせる | `uv run python -m pytest -q` |

どのツールも `--help` で引数の全部が見られる。結果の rid・hid は、そのままレース詳細・馬の過去走に渡せる。

## 実行の準備

1. `uv sync`（初回だけ。`.venv` と依存を作る）。
2. `uv` が PATH に無ければ `%LOCALAPPDATA%\Microsoft\WinGet\Packages\astral-sh.uv_Microsoft.Winget.Source_8wekyb3d8bbwe\uv.exe` を使う。
3. `uv` が無くても、`.venv\Scripts\python.exe tools\成績集計\perf.py --check` のように `.venv` の Python で直接動く。
4. 元DB の場所は既定で `../jvdata-store/jvdata.duckdb`。違う場所なら `--db PATH` か環境変数 `YOSOU_DB`。
5. 出力を保存するときは `reports/`（Git 対象外）に置く（例: `--out reports/tmp/x.csv`）。

## ツール（フォルダごと）

| フォルダ / ファイル | 用途 | 固有の引数 |
|---|---|---|
| `DBの状態/db_info.py` | DB の大きさ・表の数・中央の確定成績の期間と件数・年ごとのレース数・dm/tm/ck の開始日・同期の記録 | `--tables`（全表の行数と期間）、`--time-facts`（事実表の行数と作成秒） |
| `テーブル閲覧/table.py` | 表を1つずつ見る。列は 60 ずつ、行は `--limit`（既定 50） | `--list` `--describe` `--columns a,b` `--where 列=値`（複数可） `--offset` `--column-offset` `--from` `--to` |
| `任意SQL/sql.py` | 読むだけの SQL を1文実行（SELECT / WITH / FROM / DESCRIBE / SHOW / SUMMARIZE / EXPLAIN / PRAGMA） | 位置引数 SQL、`--file`、標準入力、`--timeout`（既定 60 秒） |
| `今週の出馬表/card.py` | 引数なしで今日以降のレースの一覧（確定前も出る）。1レースを指定すると出馬表（枠・馬番・斤量・騎手・所属・ブリンカー・馬体重・直前のマイニング順位・前走からの日数・近走着順・通算）と各馬の近走。近走は開催日より前の中央の確定成績だけ | 位置引数 rid、`--date`、`--from` `--to`、`--venue`、`--race`、`--runs`（既定 5、0 で近走なし）、`--all`（一覧の全レースの出馬表） |
| `レース一覧/races.py` | 条件に合うレース（1行 = 1レース、新しい順）。馬の条件を付けると「その馬が出たレース」 | 共通の絞り込み、`--offset` |
| `レース詳細/race.py` | 見出し・出走表と結果・ラップ・通過順・払戻。確定前の出馬表でも見られる | 位置引数 rid、または `--date --venue --race` |
| `馬の過去走/horse.py` | 馬名か hid → プロフィールと中央の過去走（新しい順）。候補が複数なら候補の表 | `--before YYYY-MM-DD`（その日より前だけ） |
| `出走検索/runners.py` | 条件を組み合わせて出走（1行 = 1頭）を検索 | 共通の絞り込み、`--sort date/odds/finish/pop`、`--offset`、`--columns`（事実表の列一覧） |
| `人気馬が負けたレース/lost_favorites.py` | 対象 = `--pop`（既定 1）と `--odds`、事象 = `--finish`（既定 `4-` = 馬券外。競走中止・失格を含む）。この3つは絞り込みではなく規則 | 共通の絞り込み、`--offset` |
| `穴馬が勝ったレース/longshot_wins.py` | 対象 = `--odds`（既定 `10-`）か `--pop`（書いた方だけ効く。両方なら AND）、事象 = `--finish`（既定 `1`） | 同上 |
| `成績集計/perf.py` | 成績7つを切り口ごとに。`reports/stats` と同じ9列 | 位置引数 切り口、`--list`、`--check`、`--top`、`--min-runs`、`--rank-by`、`--by-popularity`、`--cross 切り口`（何度でも） |
| `回収率探索/explore.py` | 条件を固定し、切り口（人気・オッズ帯・枠・前走の着順・逃げ経験・持ち時計順位 …）を1つずつ当てて、単勝か複勝の回収率が閾値以上の値を回収率順に。年ごとの回収率も添える | `--dimensions a,b`（既定は `--list` の○）、`--min-runs`（既定 30）、`--threshold`（%、既定 100）、`--target both/win/place`、`--pairs`（2つの組み合わせ、切り口 12 個まで）、`--top` |
| `傾向スコア/trend_score.py` | 1レースの傾向と採点。母集団は開催日より前の 同レース → 4つ一致 → 3つ以上一致 → 2つ以上一致。値ごとの成績を基準値と比べ、出走数が足りるいちばん狭い段で「頭向き・相手向き（+1）」「悪い（−1）」を判定する。確定前のレースにも、終わったレースにも使える | 位置引数 rid、または `--date --venue --race`。手で与える材料 `--condition` `--pops 馬番:人気,...` `--weights 馬番:馬体重:増減,...`。線引き `--scope` `--min-runs` `--good` `--bad` `--min-z`。出力 `--html [PATH]`（グラフ付きの HTML 1ファイル。既定は `reports/trend-score/`）`--open` `--detail` `--items` |
| `傾向スコア/backtest.py` | 終わったレースを同じ採点にかけて、点数の順位別・人気の順位別・点数の帯別・点数の順位×人気の帯の成績7つを出す。1レースに数秒かかる | レースを選ぶ絞り込み（`--from` は必須）、`--sample N`（等間隔に間引く）、`--no-market`（人気を使う項目を外す）、線引きは上と同じ |
| `検索画面/web.py` | ブラウザの検索画面（12タブ） | `--port`（既定 8767）、`--open`、`--idle`（既定 60 秒） |
| `合成DB/synth.py` | テスト用の小さな DB を作る（確定成績の6レースと、確定前の2レース） | `--out PATH` |

## 共通の引数と絞り込み

すべてのツールに `--db PATH`、`--format markdown|csv|json`（既定 markdown）、`--out PATH`（省略で標準出力）がある。一覧を出すツールには `--limit N` もある。

絞り込みは次の項目。**省略した条件は効かない。** 範囲は `1600`（その値）、`1400-1800`（両端を含む）、`1400-`（以上）、`-1800`（以下）と書く。

| フラグ | 形 | 例 |
|---|---|---|
| `--venue` | 競馬場の名前かコード | `東京` / `05` |
| `--surface` | 芝 / ダート / 障害 | `芝` |
| `--course` | コースの名前かトラックコード | `芝・左` / `芝・右外` / `18` |
| `--distance` | 距離（m）の範囲 | `1600` / `1400-1800` |
| `--condition` | 良 / 稍重 / 重 / 不良 | `良` |
| `--class` | クラス名 | `未勝利` / `3勝クラス` / `G1` |
| `--pop` | 単勝人気（確定）の範囲 | `1` / `1-3` |
| `--odds` | 単勝オッズ（確定、倍）の範囲 | `-1.9` / `1.5-2.9` / `20-` |
| `--frame` | 枠番の範囲 | `1-2` |
| `--finish` | 確定着順の範囲（着順の付かない競走中止・失格は含まない） | `1` / `1-3` / `4-` |
| `--sex` | 牡 / 牝 / セン | `牝` |
| `--age` | 馬齢の範囲 | `3` / `5-` |
| `--field` | 出走頭数の範囲 | `-8` / `16-18` |
| `--jockey` `--trainer` | 略称の部分一致 | `ルメール` |
| `--from` `--to` | 開催日（含む） | `2025-01-01` / `20250101` |
| `--month` | 開催月の範囲 | `6-8` |

対象は中央競馬の確定成績。出走取消・発走除外・競走除外は出走に数えない。競走中止・失格は「出走して馬券外」。馬場状態はダートのコースならダートの、芝と障害なら芝の馬場状態。

## 検索画面

`uv run python tools/検索画面/web.py --open` で `http://127.0.0.1:8767/` が開く。ダブルクリックなら `tools/検索画面/run.bat`。12のタブがある。

| タブ | できること |
|---|---|
| DB | 状態の表・年ごとのレース数・この画面の接続 |
| テーブル | 表の一覧 → 表の中身（開催日、列=値、列の一覧、行と列のページ送り） |
| 出馬表 | 今日以降のレースを日ごと・競馬場ごとに並べる → レースを押すと出馬表と各馬の近走。上のボタンで同じ日の別のレースへ。開始日を変えれば過去の日も |
| 傾向スコア | レースを選ぶ → 馬場状態・人気・馬体重を手で入れて「採点する」→ ランキング（プラス・マイナスの横棒）、グループ別の点、良い傾向・悪い傾向、要因ごとのグラフ（1着・2着・3着の率の積み上げと基準値）、同レースの過去。棒に乗ると成績7つが出る。各グラフは「表で見る」で数字になる |
| レース | 絞り込み → レースの一覧 → rid を押すと詳細（出走表・結果・ラップ・通過順・払戻）。馬名の hid から馬へ |
| 馬 | 馬名で探す → プロフィールと過去走（「この日より前」で前走の材料だけに） |
| 出走 | 絞り込みと並べ替えで出走を検索 |
| 事象 | 種類（人気馬が負けた／穴馬が勝った）と対象・事象の閾値、絞り込み |
| 条件の成績 | 条件を組み合わせて、合う出走全体の成績7つを1行で。検索した条件は下の表に並び、比べられる |
| 切り口の成績 | 切り口・組み合わせ・×人気・上位N件と絞り込み。「答え合わせ」ボタン |
| 回収率探索 | 条件を固定し、選んだ切り口を当てて回収率が閾値以上の値を探す。最少出走数・閾値・単勝／複勝・2つの組み合わせを選べる |
| SQL | 読むだけの SQL（Ctrl+Enter で実行）。本文は画面を閉じるまで残る |

- 条件はアドレスの `#` の後ろに入る。詳細へ行って戻っても、条件と結果は残る。
- 各結果の下に**同じ条件の CLI** が出る。Claude に頼むときはその行を貼ればよい。
- 「CSV を保存」で表をそのまま落とせる（Excel で開ける BOM 付き）。
- DB は使っている間だけロックし、`--idle` 秒（既定 60）放置すると手放す。jvdata-store の取得と同時に使うと「使用中」と出るので、取得が終わってから読み込み直す。

## 答え合わせとテスト

新しい集計や列を足したら、次の2つを通してから先へ進む。

```powershell
uv run python tools/成績集計/perf.py --check   # reports/stats の東京 芝・左 1600m 良 の1番人気と、成績7つが一致すること
uv run python -m pytest -q                      # 合成DB のテスト（実DB には触らない）
```

`--check` はページを作った時点の DB の期間（`--from` の既定は `成績集計/check.py` の `CHECK_DATE_FROM`、`--to` の既定は `CHECK_DATE_TO`）で比べる。DB に古い年を足しても、この期間の外は数えない。一致しないときは、確定成績の範囲（`共通/keys.py` の `FINAL_STAGES`）、異常区分 4・5 の数え方、払戻の結合の順に疑う。

## 落とし穴（元DB の約束）

- 列名は日本語で、`"開催回[第N回]"` のように角括弧を含む。SQL では必ず二重引用符で包む。
- 値は全部文字列。単勝オッズは 10 倍の4桁（`'0054'` = 5.4倍、`'0000'` は無し）。人気・着順は2桁ゼロ埋め（`'00'` は無し）。払戻金は9桁の円。
- `データ区分` で必ず絞る。同じ表に出馬表（1・2）、速報（5・6）、確定（7）、地方（A）、海外（B）が同居する。
- `競馬場コード between '01' and '10'` で中央に絞る。地方・海外が3割以上混ざっている。
- 馬場状態は `芝馬場状態コード` と `ダート馬場状態コード` に分かれ、片方しか埋まらない。
- 天候・馬場状態は `ra` から取る（`WE`・`WH` の表は無い）。
- 収録順序は当てにならない。必ず `ORDER BY` を書く（道具はすべて書いている）。
- 今週の出馬表は、木曜の夕方（出走馬名表）と金・土（出馬表）のあとに jvdata-store で取得すると入る。それまでは一覧が空。単勝オッズ・人気・直前のマイニング予想は、今の DB には確定後のものしか入っていないので、確定前のレースでは空（入れば出る）。
- 結果・払戻・確定オッズ・`dm`・`tm`・`ck`・坂路調教（`hc`）は 2016 年 1 月から、ウッドチップ調教（`wc`）は 2021 年 7 月 27 日から（2026-09 時点。それより前の行はごくわずか）。今の期間は `DBの状態/db_info.py` で確かめる。前走は、DB の最初の数か月で欠ける。
- 馬名・騎手名・オッズ・払戻など JV-Data 由来の実値を、docs・README・コミット・PR・報告に書かない。出力は標準出力か `reports/` へ。
- 傾向スコアで、確定前のレースの人気・馬場状態・馬体重は DB に無いので手で入れる。入れなければ、それを使う項目は 0 点で、馬場状態を問わない段（3つ以上一致から）で採点する。枠番・馬番は金・土の出馬表を取得すると入る。
- 同レース（同じ競走名）の過去は、DB にある年の数まで（2016 年からなら最大 10 回あまり）。平場には同レースが無い。特別戦は年によって競馬場や距離が違うことがあるので、画面の「同レースの過去」の表で確かめる。

## 部品を足すとき

- 絞り込みの項目: `共通/filters.py` の `FILTER_FIELDS` と `Filters` に1つ足す。CLI のフラグと画面のフォームは自動で増える。
- 切り口: `共通/perf.py` の `DIMENSIONS` に1つ足す。回収率探索の既定に入れるなら `共通/explore.py` の `DEFAULT_DIMENSIONS` にも足す。
- 前走・累積（逃げ経験・同コース勝利・持ち時計 …）の列は、事実表の `enriched` で「今回より前」の窓（`ROWS BETWEEN UNBOUNDED PRECEDING AND 1 PRECEDING`）から作る。当日の結果を混ぜない。
- 事実表の列: `共通/facts.py` の SQL と `FACT_COLUMNS` に足す。テスト `共通/tests/test_facts.py` が列の一覧を固定している。列の多い行のまま窓（`OVER`）を増やすと作成が遅くなるので、新しい累積は細い CTE（`narrow` → `experience`）に足す。確定前のレースの馬には `build_entry_facts` が同じ SQL を当てるので、足した列はそのまま当日の馬にも付く。
- 傾向スコアの項目: `共通/trend_items.py` の `FACTORS` に1行足す（切り口は1列のものだけ。レース後に分かる切り口は登録できない）。`傾向スコア/score-items.md` の表にも同じ ID を足す（テストが一致を見ている）。切り口に当日の材料が要るなら、`perf.Dimension` の `needs` に書く。
- 新しいツール: `tools/<日本語のツール名>/<英語のファイル名>.py`。先頭で `tools/` を `sys.path` に足し、`from 共通 import cli, db` で部品を使う。docstring の1段落目に用途と実行例を書き、この README の早見表と表に1行足す。
- テストは対象の隣の `tests/` に。合成DB（`合成DB/synth.py`）だけを使い、実DB に触らない。ファイル名は `tools/` 全体で重複させない。
