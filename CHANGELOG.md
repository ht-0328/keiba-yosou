# 変更の記録

このリポジトリの変更を、新しいものから順に書く。

- 書き方は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に沿う（**追加 / 変更 / 修正 / 削除** に分ける）。
- 利用者から見て何が変わるかを書く。内部の細かい書き換えは書かない。
- JRA-VAN Data Lab. 由来の実値（馬名・騎手名・オッズ・払戻）と、それを集計した数値は書かない。検証の結果は Git の対象外の `reports/` に置く。

## 未リリース

### 追加

- **予想モデル「レースの荒れ具合を4段階で予想」**（`src/yosou/upset_level/`）。1レースごとに、券種（単勝・馬連・3連複・3連単）ごとの荒れ具合（固い・中荒れ・大荒れ・超荒れ）の確率を出す。設計書は `docs/design/レースの荒れ具合を4段階で予想/`（01〜16）。
  - 学習データは 1行 = 1レース。目的変数は、券種ごとの払戻を切りのよい額で4段階に分けたもの（単勝 500/1,000/3,000円、馬連 1,000/3,000/1万円、3連複 3,000/1万/5万円、3連単 2万/10万/50万円。元DB の払戻の分布を調べて決めた。数値は `reports/upset_level/criteria/`）。
  - 特徴量は、レースの条件・オッズの形・出走馬の実績のばらつき・1番人気の危うさ・過去の同条件の荒れ率の 49個。1頭ごとの特徴量（手本の 71個に単勝オッズの3個を足したもの）をレース単位に集約して作る。
  - モデルは多クラス分類（4クラス）の LightGBM と CatBoost。券種ごと・3つの時点（木曜・前日・当日）ごとに学習し、4つの確率を平均する（モデルは 24個）。木曜はオッズを使わない。
  - 学習データの期間の既定は 2017年1月から（ウォームアップは 2016年。1頭単位の予想より行数が少ないため）。
  - コマンドは `uv run python -m yosou.upset_level train`（`--bet` で券種を絞れる）と `... predict <rid> --timing 当日 --odds 1:3.4 2:6.8 …`（前日・当日は全頭のオッズが要る。`--bet` で出す券種を絞れる）。モデルは `reports/upset_level/models/<券種>/<時点>/` に置く。
  - 学習の報告に、4クラスの指標（正解率・マクロF1・クラスのずれの平均・ログ損失・累積確率の AUC）と混同行列、利用者の規則（1番人気 4.0倍以上かつ 2〜5番人気 10倍未満なら荒れる）を基準にした的中率・再現率を出す。
- **予想モデル「穴馬が3着以内に入るかを予想」**（`src/yosou/longshots_in_top3/`）。穴馬1頭ずつについて「3着以内に入る確率」を出す。設計書は `docs/design/穴馬が3着以内に入るかを予想/`。
  - 穴馬は、出走頭数が 13頭以下なら 4番人気以下、14頭以上なら 6番人気以下（「人気馬が4着以下になるかを予想」の人気馬の裏返し）。学習データの行は穴馬だけで、レース内順位の特徴量はレースの全出走馬から計算する。
  - 中穴・大穴（13頭以下は 4〜6番人気と 7番人気以下、14頭以上は 6〜9番人気と 10番人気以下。一般的な相場観を調べて決めた）は、出力を絞るときにだけ使う区分で、モデルには渡さない。`predict --zone 中穴` か `--zone 大穴` で絞り、付けなければ穴馬すべてを出す。
  - 特徴量は「人気馬が4着以下になるかを予想」と同じ 75個。目的変数は「3着以内なら 1」で、「1着」の列も学習データに置く（モデルには渡さない）。
  - 予測は木曜・前日・当日の3つの時点。人気は `--pops` で全頭ぶん渡す（前日・当日は 馬番:人気、馬番がまだ無い木曜は 馬名:人気）。人気の分からない馬が1頭でもいれば止まる。
  - コマンドは `uv run python -m yosou.longshots_in_top3 train` と `... predict <rid> --timing 当日 --pops 3:1 7:2 …`。モデルは `reports/longshots_in_top3/models/` に置く。
- **予想モデル「人気馬が4着以下になるかを予想」**（`src/yosou/favorites_out_of_top3/`）。上位人気の馬1頭ずつについて「4着以下（馬券外）になる確率」を出す。設計書は `docs/design/人気馬が4着以下になるかを予想/`。
  - 人気馬は、出走頭数が 13頭以下なら 1〜3番人気、14頭以上なら 1〜5番人気（一般的な定義を調べて 14頭以上を多頭数とした）。学習データの行は人気馬だけで、レース内順位の特徴量はレースの全出走馬から計算する。
  - 特徴量は、手本の 71個に「人気順位」と「人気の履歴」（前走の人気と着順の差、近5走で人気より悪い着順だった回数、近5走の平均人気）の4個を足した 75個。単勝オッズとマイニング予想は使わない。
  - 予測は前日・当日の2つの時点。人気は `--pops 馬番:人気` で渡すか、jvdata-store の `jvstore realtime` で取り込んだ締め切り前オッズから付ける。終わったレースは確定人気で答え合わせできる。
  - コマンドは `uv run python -m yosou.favorites_out_of_top3 train` と `... predict <rid> --timing 当日 --pops 3:1 7:2`。モデルは `reports/favorites_out_of_top3/models/` に置く。
- **予想モデル「近走と適性から3着以内を予想」**（`src/yosou/form_aptitude_top3/`）。レースに出る1頭ずつについて「3着以内に入る確率」を出す。設計書は `docs/design/近走と適性から3着以内を予想/`。
  - LightGBM と CatBoost の予測確率を平均する（アンサンブル）。
  - 木曜（出走馬名表）・前日（出馬表）・当日（馬体重の発表のあと）の3つの時点ごとに、その時点で分かる特徴量だけで学習した別のモデルを使う。
  - 特徴量は 74個（レースの条件・馬・騎手と調教師・前走・近走・適性・同じレースの馬との比較・血統・調教・市場の評価）。マイニング予想は使わない。
  - 市場の評価（単勝オッズ・人気順位・オッズから見た勝率）は前日と当日に使う。学習は確定オッズ、予測は `--odds 馬番:オッズ` で渡したオッズか、jvdata-store の `jvstore realtime` で取り込んだ締め切り前のオッズを使う（終わったレースは確定オッズ）。木曜は使わない。
  - コマンドは2つ。学習は `uv run python -m yosou.form_aptitude_top3 train`、予測は `... predict <rid> --timing 前日` か `... predict <rid> --timing 当日 --odds 3:2.4 7:5.1`。
  - ハイパーパラメータは設定ファイル（TOML）で外から変えられる。書き間違えた名前はエラーにする。
  - 学習データの期間は `train` の引数で変えられる（`--warmup-from` `--train-from` `--valid-from` `--test-from`。順になっていなければエラー）。`--warmup-from` を省略すると、学習データの始まりの前の年の1月1日から。使った期間は学習の結果の表に出る。
  - 学習したモデルは Git の対象外の `reports/form_aptitude_top3/models/` に置く。
- **血統成績の道具**（`tools/血統成績/pedigree.py`）。父・父の父・母父ごとに、産駒の成績を条件別（競馬場・芝ダ・馬場状態・距離帯・コース）に出す。成績7つに加えて、「その血統の全体との差」と「その条件の全馬との差」を出す。
- 切り口を3つ追加（**芝ダ・馬場状態・距離帯**）。`成績集計/perf.py` と検索画面の「切り口の成績」タブでも選べる。距離帯は出走別着度数と同じ区切りにした。
- 合成DB に、出走別着度数（`ck`）・坂路調教（`hc`）・ウッドチップ調教（`wc`）・速報の天候馬場状態（`we`）・馬体重（`wh`）・出走取消（`av`）の表と、行を作る関数を追加。取得していない DB を再現できるよう、行が無ければ表も作らない。
- **並行で作業するときの手順**（`CLAUDE.md`）。ブランチごとに keiba-yosou の隣へ作業フォルダ（git worktree）を作り、`reports/` はメインへのつなぎ（ジャンクション）にする。複数の Claude のセッションが、同じ作業フォルダでブランチを切り替えあわないようにするため。

### 変更

- 予想モデルの共通の部品に、レース単位（1行 = 1レース）と多クラス分類のための部品を足した（荒れ具合の予想の設計書 04）。1頭ごとの特徴量をレース単位に集約して学習データを作る `RaceDatasetBuilder`・`RaceFeatureBuilder`、クラスごとの確率を返す `LightGbmMulticlassModel`・`CatBoostMulticlassModel`、レースごとの払戻を読む `RacePayoutRepository`、4クラスの当たり具合を測る `ClassModelEvaluator` と、その表。既存の `DatasetBuilder`・`FeatureBuilder` は変えていない。
  - `EnsembleModel` の平均を、確率がクラスごとの列（行数 × クラスの数）でも平均できる形にした。二値分類の結果は変わらない。
  - `TrainingWorkflow` に、モデルのクラスの並びと当たり具合の測り方を渡せるようにした。`TrainingData` に、目的変数の値の並び（`class_labels`）と、目的変数の列を持ち替える `with_label()` を足した。
  - 手本の予想だけが持っていたオッズの部品（`MarketFeatures`・`OddsInput`・`OddsResolver`）を `src/yosou/shared/` に移し、荒れ具合の予想と共通で使う。
  - 事実表（`tools/共通/facts.py`）に重量種別（`weight_type`。ハンデ / 別定 / 馬齢 / 定量）の列を足した。
  - 合成DB（`tools/合成DB/synth.py`）に、払戻の親（`hr`。券種ごとの不成立・特払・返還のフラグ）と 馬連・3連複・3連単の払戻の表を足した。架空のシーズン（`synthetic_season`）にも、人気から決めた架空の払戻を入れる。
- 予想モデルの人気に関する部品（`--pops` の人気を読む・締め切り前のオッズから人気を作る・特徴量「人気と人気の履歴」）と、目的変数「3着以内」を付ける部品を `src/yosou/shared/` に移し、3つの予想から使うようにした。多頭数の線引き（14頭）も共通の定数にした。
- 予想モデルの `--pops` に、「馬番:人気」に加えて「馬名:人気」を書けるようにした。馬番がまだ無い木曜（出走馬名表）に、見立ての人気を渡すため。馬名は出走馬名表の馬名とそのまま一致させる。
- 予想モデルの共通の `DatasetBuilder` に、出走の行から学習データの評価用の列と予測の結果に残す列（例: 穴馬の区分）を渡せるようにした。
- 予想モデル「近走と適性から3着以内を予想」に、単勝オッズを特徴量として足した（設計書 `15-decisions.md` の 3 を決め直した）。オッズを使わないモデルは、検証データで、各レースの本命が1番人気より当たらず、1番人気を除いて選び直しても2番人気より当たらず、市場と違う馬を選んだところは市場のほうが正しかったため。数値は `reports/form_aptitude_top3/exclude-favorite-valid-2025H2.txt`。足したあとは、本命の3着以内率が1番人気と同じ水準になり、1番人気と違う馬を選んだレースでは市場より当たった（`reports/form_aptitude_top3/with-odds-valid-2025H2.txt`）。
  - 特徴量の一覧（`Feature`）に「いつから分かるか」（`known_from`）を持たせ、時点ごとに使う特徴量をそこで決めるようにした。時点のクラスが持っていた「分からない特徴量の一覧」はやめた。
  - 締め切り前の単勝オッズを読む `AnnouncedOddsRepository` を、両方の予想で使うので `src/yosou/shared/repository/` に移した。
  - 予測に要る情報の確認に、単勝オッズを足した（前日・当日にオッズが無ければ、`--odds` か `jvstore realtime` を案内して止まる）。
- 予想モデルの共通部分（データを読む・特徴量を作る・学習する・設定を読む・当たり具合を測る）を `src/yosou/shared/` に移し、両方の予想から使うようにした。予想ごとに違うもの（行の選び方・目的変数・特徴量の一覧・時点の数）は、作られるときに渡す形にした。
- 予想モデルの `train` は、学習データを読み終えたら元DB を閉じてロックを手放すようにした。学習は何分もかかり、そのあいだほかの道具が「DB を使用中」で止まっていたため。
- 手本の設計書の特徴量の数を、実装に合わせて 68個から 71個に直した（血統の産駒の近1年の3着以内の割合3個。`09-features.md` の H）。
- 合成DB を作るとき、表ごとにまとめて行を入れるようにした（列の多い表で速くなる）。
- `tools/共通/cli.py` の `run` に、引数の並びを渡せるようにした（テストから実行するため）。
- `pyproject.toml`: 予想モデルが使うライブラリ（lightgbm・catboost・pandas・scikit-learn）を追加。`src` をパッケージとして入れ、テストの対象に `src` を足した。
- 設計書 `04-classes.md`・`05-sequence.md` を、実装に合わせて書き直した（1ファイル1クラス、1つの SQL につき1つのリポジトリ、フォルダ構成、リポジトリとのやりとりの図）。`01`・`02`・`06`・`14` の記述も合わせた。
- 予想モデルの学習データの期間の既定を、2024年1月から 2021年8月から（ウォームアップは 2020年）にした。3つの期間（2024年1月・2021年8月・2017年1月から）を2つの検証期間で比べ、2021年8月からがどちらでもいちばん当たったため。10年全部に延ばしても、それ以上は当たらなかった。理由は設計書 `08-training-data.md` に書いた。`min_category_count` は、どの期間でも 2000 のほうが当たったので、2000 のままにした（`12-lightgbm.md`）。
- 予想モデルの調教の特徴量で、記録が DB に無い期間（ウッドチップ調教は 2021年7月27日より前）の出走は、そのコースが関わる特徴量を欠損値（不明）にするようにした。「調教していない」と「記録が無い」を取り違えないため。学習データの期間を延ばしたときに効く（設計書 `09-features.md` の I）。
- 予想モデルの CatBoost に渡すカテゴリ特徴量の欠損値の文字列を「なし」から「不明」にした。「直近の調教のコース」の「なし」（調教なし）と混ざらないようにするため。
- 予想モデルの `min_category_count`（LightGBM で「その他」にまとめる出走数）の初期値を 50 から 2000 にした。50 のままだと、LightGBM が判断の半分を騎手・調教師・血統の名前に置いて偶然を覚え込み、学習に使っていない期間での当たり具合が下がっていたため。理由と、学習期間を延ばしたときの見直し方は設計書 `12-lightgbm.md` に書いた。
- `tools/README.md` と `tools/傾向スコア/score-items.md` の、DB に入っている期間の説明を、10年ぶん（2016年から）に広げたあとの内容に直した。

### 修正

- 予想モデルの LightGBM の保存ファイルに、エンコーダーをオブジェクトのまま pickle していたのをやめ、素のデータ（列の並びとカテゴリの一覧）で書くようにした。共通部分を `shared/` に移したあと、前に学習したモデルがクラスの置き場所の違いで読めなくなっていたため。古い形式のファイルは「train で学習し直してください」と案内して止まる（学習し直しが要る）。
- `成績集計/perf.py --check` が、DB を 2016年からに広げたあと不一致になっていた。基準のページ（`reports/stats`）を作ったときの DB の期間（2023年1月から）だけで数えるようにした。
