# 変更の記録

このリポジトリの変更を、新しいものから順に書く。

- 書き方は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に沿う（**追加 / 変更 / 修正 / 削除** に分ける）。
- 利用者から見て何が変わるかを書く。内部の細かい書き換えは書かない。
- JRA-VAN Data Lab. 由来の実値（馬名・騎手名・オッズ・払戻）と、それを集計した数値は書かない。検証の結果は Git の対象外の `reports/` に置く。

## 未リリース

### 追加

- **道具「当日の予想」**（`tools/当日の予想/`）。今日の、今より後に発走するレースを `custom_binary` のモデル（馬柱 + 券種オッズ、オッズを出発点）で予想し、複勝の期待値が 1.2 以上の馬を「買い」として一覧にする。`run.bat` をダブルクリックで動く。馬体重が発表済みのレースは「馬体重あり」、まだなら「馬体重なし」のモデルを使う。モデルが無ければ初回に学習する。結果は `reports/当日の予想/` にも書く。先に jvdata-store の `realtime_today.bat` で速報を取り込んでおく。
- **予想モデル「特徴量と条件を選んで予想」**（`src/yosou/custom_binary/`）。特徴量・目的・時点・対象の馬を利用者が選んで、LightGBM と CatBoost の二値分類を作る。設計書は `docs/design/特徴量と条件を選んで予想/`（01〜16）、使い方は `docs/custom_binary.md`。
  - 1つのモデルの作り方を YAML の設定ファイルに、使う特徴量を別のテキストに1行1つ書く。目的は馬券内（1〜3着）・馬券外（4着以下と中止・失格）・勝利（1着）から、時点は木曜・前日・当日から1つを選ぶ。その時点で分からない特徴量を選ぶと、学習の前に止める。
  - 対象の馬は、人気範囲（`popularity`）と、特徴量の値の条件（`conditions`。例: 距離 1400m 以下、東京、馬体重 440kg 以下）で絞れる。特徴量は全頭で作ってから絞る。
  - `odds_baseline: true` で、単勝オッズから見た確率を出発点にして、特徴量からの上げ下げだけを学べる（前日・当日のみ）。
  - 選べる特徴量は 90個（手本の 71個、人気の履歴 4個、単勝オッズから見た評価 3個、券種オッズ 12個）。券種オッズは、3連単・馬単・3連複・馬連・ワイド・複勝のオッズから見た馬ごとの確率と、その単勝オッズとの差。特徴量はクラスを1つ書いて登録の一覧に1行足せば増やせる。
  - 学習と評価の結果に、単勝・複勝の回収率（対象の全頭・各レースの確率1位・期待値の線ごと。複勝は想定払戻倍率で期待値を出し、開催日単位の回収率の下限も出す）を出す。
  - コマンドは `uv run python -m yosou.custom_binary features / train / predict / evaluate`。モデルは `reports/custom_binary/<設定の name>/` に保存し、同じ名前は上書きしない。
  - 券種オッズの特徴量を使うモデルで発売中のレースを予想するには、jvdata-store の `jvstore realtime`（全賭式のオッズも取る）で取り込んでおく。
  - `predict` の結果に、その時点のオッズでの期待値（勝利は確率×単勝オッズ、馬券内・馬券外は3着以内の確率×複勝の想定払戻倍率）を出す。想定払戻倍率は学習のときに `place_price.json` として保存する。
- **研究「特徴量の組み合わせ探索」**（`research/特徴量の組み合わせ探索/`）。`custom_binary` を使って、回収率 100% を超える特徴量の組み合わせを探す。見つける期間（2017〜2022）→ 確かめる期間（2023〜2024）→ テスト期間（2025〜）の順に、事前に決めた基準で採否を決める。結論は `docs/結果.md`、数値は Git の対象外の `reports/特徴量の組み合わせ探索/`。
  - レースの区分 × 馬の条件 × 人気帯のルール（約22万通り）と、`custom_binary` のモデルで探した。馬柱の特徴量だけの組み合わせは、テスト期間で 100% を安定して超えなかった。券種オッズの特徴量を足したモデルは、テスト期間で複勝の回収率が 100% を超えたが、点数が少なく、事前の採用基準はすべてを満たしていない。

- **予想モデル「1番人気を買うか消すかを予想」**（`src/yosou/favorite_buy_or_fade/`）。1番人気1頭ずつを「消す」「単勝と複勝を買う」「複勝だけ買う」に分ける。設計書は `docs/design/1番人気を買うか消すかを予想/`（01〜16）。
  - 1番人気だけを、勝利（1着）・馬券内（1〜3着。勝った馬も含む）・馬券外（4着以下。競走中止・失格を含む）の3つのグループに分け、芝ダート × 距離の単位ごとに、グループごとのモデルをそのグループの馬だけで作る（k近傍法）。1番人気が少ない距離は、同じ芝ダートの近い距離とまとめる。
  - 対象の1番人気が各グループにどれだけ近いかを 0〜100 の点数で出す。点数は、同じ単位の1番人気全員と比べた順位にそろえる（頭数の違うグループどうしで比べられるように）。
  - 判定は、馬券外の点数が馬券内の点数より高ければ「消す」。消さなかった馬は、勝利の点数が馬券内の点数より高ければ「単勝と複勝」、そうでなければ「複勝だけ」。
  - **オッズなしで予想する。** 今回の単勝オッズから作る特徴量（まとまり K）は、近さの計算に使わない（重み 0）。人気を使うのは、1番人気を選ぶところと、人気の履歴（前走の人気など、過去のレースの人気）だけ。設定で K の重みを 1 にすると、オッズも使える。
  - 特徴量の一覧は「人気馬が4着以下になるかを予想」と同じ 78個（前走の人気を含む）。騎手・調教師・血統の名前は距離を測れないので、強さは数字の列（近1年の3着以内の割合など）で渡す。LightGBM と CatBoost は、1つのグループだけのデータでは学習できないので使わない。
  - 方針（単位のまとめ方・特徴量の時点・k・まとまりごとの重み・判定の線・掛け金・評価の年）は `setting/default_settings.toml` にまとめた。変えたい項目だけを書いた TOML を `--config` で渡して、学習し直せる（1回1分ほど）。
  - コマンドは3つ。`uv run python -m yosou.favorite_buy_or_fade evaluate`（評価する年ごとに、その前年までで学習し直して判定し、年ごと・判定ごと・単位ごとの表を出す）、`... train`（本番用に学習して `reports/favorite_buy_or_fade/models/` に保存）、`... predict <rid> --odds 3:2.4 …`（1レースの1番人気の点数と判定）。

- **研究「既存モデルの改善」**（`research/既存モデルの改善/`）。既存の4つの予想を、ワークスペースの `plans/既存モデルの修正計画.md` に沿って直し、直す前・直した後・オッズだけの基準を、同じ7つの区切り（2023年前半〜2026年。過去で学習して次の半年を予想する）で比べる。直した予想を組み合わせた馬券の買い方（7券種）も、同じ区切りで確かめる。進め方は `docs/01-進め方.md`、結果の読み方は `docs/02-結果の読み方.md`、数値は Git の対象外の `reports/既存モデルの改善/`。
  - 入口は `build_tables.py`（学習データ）・`walk_forward.py`（区切りごとの学習と予測）・`upset_calc.py`（荒れ具合を着順の確率から計算）・`compare.py`（比べ方の表。`--only form_experiments` で材料の実験の採否）・`backtest.py`（組み合わせた買い方）・`build_experiments.py`（材料を1つずつ足す実験の表）。
  - 採用の基準は計画のとおり。全頭はオッズだけより確率の誤差（ログ損失）が小さい区切りが7つのうち5つ以上、材料の実験は直した全頭の予想より小さい区切りが5つ以上で、全期間を合わせても小さいもの。
- **研究「回収率100超」**（`research/回収率100超/`）。券種ごとの投票プールの売上の差を使って、回収率 100% を超える買い方を探す研究。2016年からの中央競馬 35,695 レースで検証した。
  - **考え方。** JRA は券種ごとに別々の投票プールを持ち、売上の大きさが数倍違う（3連単がいちばん大きく、単勝・複勝が小さい）。売上の大きいプールのオッズほど多くの判断が入っていて正確である。そこで **3連単・3連複・馬単・馬連・ワイドのオッズから馬ごとの確率を取り出し、それを使って単勝・複勝を買う**。情報を取り出す場所と賭ける場所を分ける。
  - 元DB から読むのは `analysis/repository/`（1クラス = 1 SQL）。出走 × 確定オッズ × 払戻、券種プールごとの馬ごとの確率、3連単・馬単の着順ごとの確率、票数の配分、プールの大きさ、マイニング予想、事実表の実力の列。
  - 市場の歪み（人気薄が買われすぎる偏り）は、`log` の折れ線とレース単位の条件付きロジットで直す（べき乗の補正では直しきれなかった）。勝率から複勝・ワイド・3連複の確率を出すには Stern 補正つきの Harville を使う（補正なしでは人気馬を高く見積もりすぎた）。
  - 確率は LightGBM と CatBoost の平均。検証はウォークフォワード（その年より前で学習 → その年で予測）で、2019年から1年ずつ。
  - 買い方は複勝の期待値で決める。期待値の線は前半の年（2019〜2021）の結果だけで決め、後半（2022〜2026）で確かめた。回収率は開催日を単位にしたブートストラップの幅つきで出す。
  - 入口は `extract.py`（元DB から中間データを作る）と `backtest.py`（学習と検証）。数値は `reports/回収率100超/`（Git 対象外）。
  - **分かったこと。** 騎手・調教師・血統をカテゴリのまま特徴量にすると当たり具合が悪化する（名前を覚えるだけになる）。「市場の期待に対する超過成績」の数値に変えると悪化はしないが、ほとんど効かない。既存の4つの予想が使う特徴量は、市場への上乗せがほとんど無い。
  - **残っている課題。** 検証は確定オッズを使っている。実運用には締め切り前のオッズが要るが、jvdata-store が取っている速報オッズは `0B31`（単勝・複勝・枠連）だけで、この買い方が要る馬連・ワイド・馬単・3連複・3連単のオッズ（`0B30` の全賭式、または `0B32`〜`0B36`）を取っていない。

- **研究「馬券の買い方の検証」**（`research/馬券の買い方の検証/`）。4つの予想モデルの当日時点の予測を期間ぶん一括で出し（`predict_all.py`）、ルール集の買い方（17種）× 参加パターン（全レース・荒れるレースだけ・自信のあるレースだけ・両方・毎週上位・毎週 + 重賞）× しきい値の格子 の戦略を、確定オッズと払戻で精算・探索・確認する（`backtest.py`）。探索は検証期間、確認はテスト期間で1回だけ。採否の基準は `docs/03-protocol.md` に事前に固定。1回目の結果の読み方は `docs/04-results.md`（数値は `reports/馬券の買い方の検証/`）。
  - 2回目の検証で、確率の値で絞る買い方 16種（穴馬モデルの確率がしきい値以上の馬を全部、確率 × 複勝オッズの期待値がしきい値以上の馬）と、採否の基準「月別の回収率の中央値」を足した。材料表に複勝の確定オッズ（下限）が付く。
  - 研究のテストを `pytest` の対象に足した（`pyproject.toml` の `testpaths`・`pythonpath` に `research`）。
  - 合成DB（`tools/合成DB/synth.py`）に、オッズの親と子（`o1`〜`o6`）とワイド・馬単の払戻の表を足した（行があるときだけ作る）。
- **馬券の買い方のルール集**（`docs/rules/馬券の買い方/`、01〜13）。券種ごと（単勝・複勝・馬連・馬単・ワイド・3連複・3連単・WIN5）と重ね買い（券種の組み合わせ）に、買い方を「いつ・どうする・なぜ・判定に使う情報」の形のルール（ID は TAN-01 のように券種の略号と番号）と、列ごとの頭数が決まった買い目の型（SRF-P01 のように P 付き）にまとめた。全券種に共通するルール（ALL-01〜10）、券種の比較、用語の意味、ルール同士の食い違い、予想モデルとの対応、ルールの根拠になっている統計の主張（STAT-01〜24。元DB で確かめる対象）、このルール集の課題（未検証のルールの確かめ方、食い違いの決め方、騎手名の扱いなど）も書いた。まだ検証もプログラム化もしていない。
- **予想モデル「レースの荒れ具合を4段階で予想」**（`src/yosou/upset_level/`）。1レースごとに、券種（単勝・馬連・3連複・3連単）ごとの荒れ具合（固い・中荒れ・大荒れ・超荒れ）の確率を出す。設計書は `docs/design/レースの荒れ具合を4段階で予想/`（01〜16）。
  - 学習データは 1行 = 1レース。目的変数は、券種ごとの払戻を切りのよい額で4段階に分けたもの（単勝 500/1,000/3,000円、馬連 1,000/3,000/1万円、3連複 3,000/1万/5万円、3連単 2万/10万/50万円。元DB の払戻の分布を調べて決めた。数値は `reports/upset_level/criteria/`）。
  - 特徴量は、レースの条件・オッズの形・出走馬の実績のばらつき・1番人気の危うさ・過去の同条件の荒れ率の 49個。1頭ごとの特徴量（手本の 71個に単勝オッズの3個を足したもの）をレース単位に集約して作る。
  - モデルは多クラス分類（4クラス）の LightGBM と CatBoost。券種ごと・3つの時点（木曜・前日・当日）ごとに学習し、4つの確率を平均する（モデルは 24個）。木曜はオッズを使わない。
  - 学習データの期間の既定は 2017年1月から（ウォームアップは 2016年。1頭単位の予想より行数が少ないため）。
  - コマンドは `uv run python -m yosou.upset_level train`（`--bet` で券種を絞れる）と `... predict <rid> --timing 当日 --odds 1:3.4 2:6.8 …`（前日・当日は全頭のオッズが要る。`--bet` で出す券種を絞れる）。モデルは `reports/upset_level/models/<券種>/<時点>/` に置く。
  - 学習の報告に、4クラスの指標（正解率・マクロF1・クラスのずれの平均・ログ損失・累積確率の AUC）と混同行列、利用者の規則（1番人気 4.0倍以上かつ 2〜5番人気 10倍未満なら荒れる）を基準にした的中率・再現率を出す。
- **予想モデル「穴馬が3着以内に入るかを予想」**（`src/yosou/longshots_in_top3/`）。穴馬1頭ずつについて「3着以内に入る確率」を出す。設計書は `docs/design/穴馬が3着以内に入るかを予想/`。
  - 穴馬は、出走頭数が 13頭以下なら 4番人気以下、14頭以上なら 6番人気以下（「人気馬が4着以下になるかを予想」の人気馬の裏返し）。学習データの行は穴馬だけで、レース内順位の特徴量はレースの全出走馬から計算する。
  - 中穴・大穴（13頭以下は 4〜6番人気と 7番人気以下、14頭以上は 6〜9番人気と 10番人気以下。一般的な相場観を調べて決めた）は、出力を絞るときにだけ使う区分で、モデルには渡さない。`predict --zone 中穴` か `--zone 大穴` で絞り、付けなければ穴馬すべてを出す。
  - 特徴量は「人気馬が4着以下になるかを予想」と同じ 75個。目的変数は「3着以内なら 1」で、「1着」の列も学習データに置く（モデルには渡さない）。
  - 予測は木曜・前日・当日の3つの時点。人気は `--pops` で全頭ぶん渡す（前日・当日は 馬番:人気、馬番がまだ無い木曜は 馬名:人気）。人気の分からない馬が1頭でもいれば止まる。
  - コマンドは `uv run python -m yosou.longshots_in_top3 train` と `... predict <rid> --timing 当日 --pops 3:1 7:2 …`。モデルは `reports/longshots_in_top3/models/` に置く。
- **予想モデル「人気馬が4着以下になるかを予想」**（`src/yosou/favorites_out_of_top3/`）。上位人気の馬1頭ずつについて「4着以下（馬券外）になる確率」を出す。設計書は `docs/design/人気馬が4着以下になるかを予想/`。
  - 人気馬は、出走頭数が 13頭以下なら 1〜3番人気、14頭以上なら 1〜5番人気（一般的な定義を調べて 14頭以上を多頭数とした）。学習データの行は人気馬だけで、レース内順位の特徴量はレースの全出走馬から計算する。
  - 特徴量は、手本の 71個に「人気順位」と「人気の履歴」（前走の人気と着順の差、近5走で人気より悪い着順だった回数、近5走の平均人気）の4個を足した 75個。単勝オッズとマイニング予想は使わない。
  - 予測は前日・当日の2つの時点。人気は `--pops 馬番:人気` で渡すか、jvdata-store の `jvstore realtime` で取り込んだ締め切り前オッズから付ける。終わったレースは確定人気で答え合わせできる。
  - コマンドは `uv run python -m yosou.favorites_out_of_top3 train` と `... predict <rid> --timing 当日 --pops 3:1 7:2`。モデルは `reports/favorites_out_of_top3/models/` に置く。
- **予想モデル「近走と適性から3着以内を予想」**（`src/yosou/form_aptitude_top3/`）。レースに出る1頭ずつについて「3着以内に入る確率」を出す。設計書は `docs/design/近走と適性から3着以内を予想/`。
  - LightGBM と CatBoost の予測確率を平均する（アンサンブル）。
  - 木曜（出走馬名表）・前日（出馬表）・当日（馬体重の発表のあと）の3つの時点ごとに、その時点で分かる特徴量だけで学習した別のモデルを使う。
  - 特徴量は 74個（レースの条件・馬・騎手と調教師・前走・近走・適性・同じレースの馬との比較・血統・調教・市場の評価）。マイニング予想は使わない。
  - 市場の評価（単勝オッズ・人気順位・オッズから見た勝率）は前日と当日に使う。学習は確定オッズ、予測は `--odds 馬番:オッズ` で渡したオッズか、jvdata-store の `jvstore realtime` で取り込んだ締め切り前のオッズを使う（終わったレースは確定オッズ）。木曜は使わない。
  - コマンドは2つ。学習は `uv run python -m yosou.form_aptitude_top3 train`、予測は `... predict <rid> --timing 前日` か `... predict <rid> --timing 当日 --odds 3:2.4 7:5.1`。
  - ハイパーパラメータは設定ファイル（TOML）で外から変えられる。書き間違えた名前はエラーにする。
  - 学習データの期間は `train` の引数で変えられる（`--warmup-from` `--train-from` `--valid-from` `--test-from`。順になっていなければエラー）。`--warmup-from` を省略すると、学習データの始まりの前の年の1月1日から。使った期間は学習の結果の表に出る。
  - 学習したモデルは Git の対象外の `reports/form_aptitude_top3/models/` に置く。
- **血統成績の道具**（`tools/血統成績/pedigree.py`）。父・父の父・母父ごとに、産駒の成績を条件別（競馬場・芝ダ・馬場状態・距離帯・コース）に出す。成績7つに加えて、「その血統の全体との差」と「その条件の全馬との差」を出す。
- 切り口を3つ追加（**芝ダ・馬場状態・距離帯**）。`成績集計/perf.py` と検索画面の「切り口の成績」タブでも選べる。距離帯は出走別着度数と同じ区切りにした。
- 合成DB に、出走別着度数（`ck`）・坂路調教（`hc`）・ウッドチップ調教（`wc`）・速報の天候馬場状態（`we`）・馬体重（`wh`）・出走取消（`av`）の表と、行を作る関数を追加。取得していない DB を再現できるよう、行が無ければ表も作らない。
- **並行で作業するときの手順**（`CLAUDE.md`）。ブランチごとに keiba-yosou の隣へ作業フォルダ（git worktree）を作り、`reports/` はメインへのつなぎ（ジャンクション）にする。複数の Claude のセッションが、同じ作業フォルダでブランチを切り替えあわないようにするため。

### 変更

- 研究「既存モデルの改善」の組み合わせた買い方（`backtest.py`）を、利用者の考え方に沿って作り替えた。それまでは全部の組み合わせの
  期待値で買い目を選んでいたため、8割のレースで勝負し、券種ごとに違う馬を買い、検証期間でたまたま大当たりした買い方が選ばれていた。
  - 馬ごとに単勝・複勝の期待値を出し、役割を決める（`analysis/horse_roles/`）。本当に危険な1番人気だけを消し、軸は3着以内の確率が
    いちばん高い馬（2番目も高ければ2頭軸）、◎は単勝の期待値がいちばん高い馬。
  - 券種ごとの買い目（`analysis/ticket_combos/`）: 馬連・ワイド・馬単は人気 + 穴（人気-人気は1点、荒れそうなら穴-穴も）、
    3連複は軸から 1点 300円、3連単は◎か軸を1着に 1点 100円で広め（最大30点）。単勝・複勝・2頭の券種は予算を払戻均等で分ける。
  - 当たる確率のずれを、検証期間の券種 × オッズの帯ごとの比で直してから期待値を出す（`ProbabilityCalibrator`）。
  - 期待値 1 未満の買い目と、トリガミになる買い目は外す。1レースでは全部の券種を買わず、券種全体の期待値の高い券種から
    1レース 5,000円の予算に入るところまで買う。勝負するのは1開催日の上位 N レース（1番人気を消したレースを先に）と重賞。
  - 線は、テストの直前の1年で、当たり30回以上の線の中から回収率の控えめな見積もりで選ぶ。
  - 結果の表は金額で数え、券種ごとの勝負したレース数・点数・的中数、組み合わせの種類、レースの条件ごと、確率のずれ（補正前と補正後）を出す。
- 予想モデル「近走と適性から3着以内を予想」「穴馬が3着以内に入るかを予想」「人気馬が4着以下になるかを予想」を、既存モデルの修正計画に沿って直した（設計書の `15-decisions.md` に「既存モデルの修正計画での直し」を足した）。**モデルの置き場所と中身が変わったので、`train` で学習し直すまで `predict` は動かない。**
  - オッズから作った基準の確率（単勝オッズから Harville の式で出した3着以内率。人気馬は 1 − それ）を出発点にし、モデルは基準からの上げ下げだけを学ぶ（LightGBM の `init_score`、CatBoost の `baseline`）。木曜はオッズが無いので基準なしで学ぶ。基準を使ったかは保存ファイルに残し、基準を使って学んだモデルに基準の無いデータを渡すと止まる。
  - 全頭の予想の特徴量に「オッズから見た3着以内率」を、穴馬と人気馬の予想に単勝オッズから見た評価の3個（まとまり K）を足した。穴馬と人気馬の `predict` は `--odds 馬番:オッズ` を受け取る（`--pops` を省くと、オッズの小さい順を人気にする）。
  - 穴馬の予想は中穴と大穴で、人気馬の予想は 1番人気・2〜3番人気・4〜5番人気で、別のモデルを学習する（`src/yosou/shared/workflow/segmented_training.py`・`segmented_prediction.py`）。
  - 目的変数「3着以内」は残し、払戻と照合した「複勝的中」の列を学習データに足した（7頭以下は2着まで、同着は払戻どおり、4頭以下は複勝を売らない）。複勝の最低・最高オッズを読む `PlaceOddsRepository` を足した。
  - 全頭と穴馬の予測の表に、複勝的中の確率と複勝の期待値（複勝的中の確率 × 見込みの払戻の倍率）を足した。見込みの倍率は `train` のあとに学習データで決めて、モデルの置き場所に保存する（`src/yosou/shared/place_value/`）。
  - 人気馬の予測の表に、オッズから見た4着以下の確率・危険度（予想 − それ）・危険の判定を足した。危険の線は `train` のあとに、時点ごと・人気帯ごとに検証データで決めて保存する（`src/yosou/favorites_out_of_top3/danger/`）。
- 予想モデルの共通の部品に、レース単位（1行 = 1レース）と多クラス分類のための部品を足した（荒れ具合の予想の設計書 04）。1頭ごとの特徴量をレース単位に集約して学習データを作る `RaceDatasetBuilder`・`RaceFeatureBuilder`、クラスごとの確率を返す `LightGbmMulticlassModel`・`CatBoostMulticlassModel`、レースごとの払戻を読む `RacePayoutRepository`、4クラスの当たり具合を測る `ClassModelEvaluator` と、その表。既存の `DatasetBuilder`・`FeatureBuilder` は変えていない。
  - `EnsembleModel` の平均を、確率がクラスごとの列（行数 × クラスの数）でも平均できる形にした。二値分類の結果は変わらない。
  - `TrainingWorkflow` に、モデルのクラスの並びと当たり具合の測り方を渡せるようにした。`TrainingData` に、目的変数の値の並び（`class_labels`）と、目的変数の列を持ち替える `with_label()` を足した。
  - 手本の予想だけが持っていたオッズの部品（`MarketFeatures`・`OddsInput`・`OddsResolver`）を `src/yosou/shared/` に移し、荒れ具合の予想と共通で使う。
  - 事実表（`tools/共通/facts.py`）に重量種別（`weight_type`。ハンデ / 別定 / 馬齢 / 定量）の列を足した。
  - 合成DB（`tools/合成DB/synth.py`）に、払戻の親（`hr`。券種ごとの不成立・特払・返還のフラグ）と 馬連・3連複・3連単の払戻の表を足した。架空のシーズン（`synthetic_season`）にも、人気から決めた架空の払戻を入れる。
- 予想モデルの学習の報告に「人気の基準との比べ方」の表を足した。各レースで確率1位の馬と、人気がいちばん上の馬（全頭の予想なら 1番人気、穴馬の予想なら 4番か 6番人気）の、目的変数が 1 だった割合と複勝回収率を並べ、同じ人気の馬どうしで比べた AUC も出す。モデルが人気順をなぞるだけになっていないかを見るため（設計書は `docs/design/穴馬が3着以内に入るかを予想/16-evaluation.md`。期間の分け方と「買い」の線引きもここで決めた）。1頭ごとの予想の指標で、荒れ具合の予想（1行 = 1レース）は4クラスの指標を使う。
- 予想モデルの人気に関する部品（`--pops` の人気を読む・締め切り前のオッズから人気を作る・特徴量「人気と人気の履歴」）と、目的変数「3着以内」を付ける部品を `src/yosou/shared/` に移し、3つの予想から使うようにした。多頭数の線引き（14頭）も共通の定数にした。
- 予想モデルの `--pops` に、「馬番:人気」に加えて「馬名:人気」を書けるようにした。馬番がまだ無い木曜（出走馬名表）に、見立ての人気を渡すため。馬名は出走馬名表の馬名とそのまま一致させる。
- 予想モデルの共通の `DatasetBuilder` に、出走の行から学習データの評価用の列と予測の結果に残す列（例: 穴馬の区分）を渡せるようにした。
- 予想モデル「近走と適性から3着以内を予想」に、単勝オッズを特徴量として足した（設計書 `15-decisions.md` の 3 を決め直した）。オッズを使わないモデルは、検証データで、各レースの本命が1番人気より当たらず、1番人気を除いて選び直しても2番人気より当たらず、市場と違う馬を選んだところは市場のほうが正しかったため。数値は `reports/form_aptitude_top3/exclude-favorite-valid-2025H2.txt`。足したあとは、本命の3着以内率が1番人気と同じ水準になり、1番人気と違う馬を選んだレースでは市場より当たった（`reports/form_aptitude_top3/with-odds-valid-2025H2.txt`）。
  - 特徴量の一覧（`Feature`）に「いつから分かるか」（`known_from`）を持たせ、時点ごとに使う特徴量をそこで決めるようにした。時点のクラスが持っていた「分からない特徴量の一覧」はやめた。
  - 締め切り前の単勝オッズを読む `AnnouncedOddsRepository` を、両方の予想で使うので `src/yosou/shared/repository/` に移した。
  - 予測に要る情報の確認に、単勝オッズを足した（前日・当日にオッズが無ければ、`--odds` か `jvstore realtime` を案内して止まる）。
- 予想モデルの共通部分（データを読む・特徴量を作る・学習する・設定を読む・当たり具合を測る）を `src/yosou/shared/` に移し、両方の予想から使うようにした。予想ごとに違うもの（行の選び方・目的変数・特徴量の一覧・時点の数）は、作られるときに渡す形にした。
- 予想モデルの `train` は、学習データを読み終えたら元DB を閉じてロックを手放すようにした。学習は何分もかかり、そのあいだほかの道具が「DB を使用中」で止まっていたため。
- 手本の設計書の特徴量の数を、実装に合わせて 68個から 71個に直した（血統の産駒の近1年の3着以内の割合3個。`09-features.md` の H）。
- 合成DB を作るとき、表ごとにまとめて行を入れるようにした（列の多い表で速くなる）。
- `tools/共通/cli.py` の `run` に、引数の並びを渡せるようにした（テストから実行するため）。
- `pyproject.toml`: 予想モデルが使うライブラリ（lightgbm・catboost・pandas・scikit-learn）を追加。`src` をパッケージとして入れ、テストの対象に `src` を足した。
- 設計書 `04-classes.md`・`05-sequence.md` を、実装に合わせて書き直した（1ファイル1クラス、1つの SQL につき1つのリポジトリ、フォルダ構成、リポジトリとのやりとりの図）。`01`・`02`・`06`・`14` の記述も合わせた。
- 予想モデルの学習データの期間の既定を、2024年1月から 2021年8月から（ウォームアップは 2020年）にした。3つの期間（2024年1月・2021年8月・2017年1月から）を2つの検証期間で比べ、2021年8月からがどちらでもいちばん当たったため。10年全部に延ばしても、それ以上は当たらなかった。理由は設計書 `08-training-data.md` に書いた。`min_category_count` は、どの期間でも 2000 のほうが当たったので、2000 のままにした（`12-lightgbm.md`）。
- 予想モデルの調教の特徴量で、記録が DB に無い期間（ウッドチップ調教は 2021年7月27日より前）の出走は、そのコースが関わる特徴量を欠損値（不明）にするようにした。「調教していない」と「記録が無い」を取り違えないため。学習データの期間を延ばしたときに効く（設計書 `09-features.md` の I）。
- 予想モデルの CatBoost に渡すカテゴリ特徴量の欠損値の文字列を「なし」から「不明」にした。「直近の調教のコース」の「なし」（調教なし）と混ざらないようにするため。
- 予想モデルの `min_category_count`（LightGBM で「その他」にまとめる出走数）の初期値を 50 から 2000 にした。50 のままだと、LightGBM が判断の半分を騎手・調教師・血統の名前に置いて偶然を覚え込み、学習に使っていない期間での当たり具合が下がっていたため。理由と、学習期間を延ばしたときの見直し方は設計書 `12-lightgbm.md` に書いた。
- `tools/README.md` と `tools/傾向スコア/score-items.md` の、DB に入っている期間の説明を、10年ぶん（2016年から）に広げたあとの内容に直した。

### 修正

- 予想モデルの LightGBM の保存ファイルに、エンコーダーをオブジェクトのまま pickle していたのをやめ、素のデータ（列の並びとカテゴリの一覧）で書くようにした。共通部分を `shared/` に移したあと、前に学習したモデルがクラスの置き場所の違いで読めなくなっていたため。古い形式のファイルは「train で学習し直してください」と案内して止まる（学習し直しが要る）。
- `成績集計/perf.py --check` が、DB を 2016年からに広げたあと不一致になっていた。基準のページ（`reports/stats`）を作ったときの DB の期間（2023年1月から）だけで数えるようにした。
