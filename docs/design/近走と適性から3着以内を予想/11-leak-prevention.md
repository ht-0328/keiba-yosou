# 11 リークを防ぐ決まり

**この文書で決めること:** 学習では当たるのに本番では当たらないモデル（リーク）にならないよう、特徴量を作るときに守る決まり。

**結論: 特徴量は、予測する時点（木曜・前日・当日。[07-prediction-timing.md](07-prediction-timing.md)）で分かることだけから作る。**

リークがあると、検証では当たるのに本番では当たらないモデルになる。用語の意味は [02-glossary.md](02-glossary.md) を参照。予測する時点は [07-prediction-timing.md](07-prediction-timing.md) を参照。

## 決まり

1. **そのレースの結果は特徴量にしない。** 確定着順・走破タイム・着差・上がり3F・通過順・脚質・払戻・異常区分である。事実表の列でいうと `finish` `finish_time` `time_diff` `last3f` `last3f_rank` `corner4` `style` `winner_style` `win_payout` `place_payout` `abnormal`。
2. **過去走から作る特徴量（近走・同じコースでの成績・3着以内の割合・持ち時計）と調教の特徴量は、開催日より前のものだけから計算する。** 同じ日の先に終わったレースも使わない。
3. **マスタにある累計の成績は使わない。** 競走馬マスタ・騎手マスタ・調教師マスタの「本年」「累計」の成績は、今日時点の値である。過去のサンプルに付けると、そのあとの成績が混ざる。出走別着度数（`ck`）は、出走ごとに出走馬名表の時点の値を持つので、使ってよい。
4. **学習データと予測用データは、同じプログラムで作る。** 作り方が別だと、同じ名前の特徴量でも中身がずれる。
5. **確定オッズは、結果ではなく「締め切り時点の市場の見方」なので、特徴量に使ってよい。ただし、予測では締め切り前のオッズになる。** 学習データの単勝オッズ（`win_odds`）は確定オッズ、予測のときは利用者が渡したオッズか締め切り前のオッズで、値が少しずれる（[07-prediction-timing.md](07-prediction-timing.md#予測のときのオッズの与え方)）。当日の締め切り直前に出し直すとずれは小さくなる。確定人気（`popularity`）は特徴量にせず、人気順位はオッズから付け直す。こうすると、学習でも予測でも「そのときのオッズの小さい順」という同じ作り方になる。

## 文書情報

| 項目 | 内容 |
|---|---|
| 作成日 | 2026-09-21 |
| 更新 | 2026-09-23: 単勝オッズを特徴量にしたので、決まり 1 から確定オッズ・確定人気を外し、決まり 5 を足した |
