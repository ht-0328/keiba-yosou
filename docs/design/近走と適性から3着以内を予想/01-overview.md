# 01 近走と適性から3着以内を予想する: 概要

**近走の内容と適性から、レースに出る1頭ずつについて「3着以内に入る確率」を出す予想である。**
適性とは、そのコース・距離・馬場に向いているかどうかで、同じ条件での過去の成績・持ち時計・血統から見る。
**オッズ・人気と、JRA-VAN のマイニング予想は使わない。**

- 作り方: 機械学習の二値分類。LightGBM と CatBoost の2つのモデルで予測確率を出し、平均する（アンサンブル）。
- 予測する時点: 木曜（出走馬名表）・前日（出馬表）・当日（馬体重の発表のあと）の3つのどれでも出せる。時点ごとに、その時点で分かる特徴量だけで学習した別のモデルを使う（[07-prediction-timing.md](07-prediction-timing.md)）。
- データの元: JRA-VAN Data Lab. の中央競馬のデータ（jvdata-store が DuckDB に貯めたもの）。
- 用語の意味: [02-glossary.md](02-glossary.md) を参照。
- 状態: 決めてほしいことは決まり（[15-decisions.md](15-decisions.md)）、プログラムも作った（`src/yosou/form_aptitude_top3/`。クラスとフォルダは [04-classes.md](04-classes.md)）。学習・検証・テストの期間の分け方、評価指標、平均のしかたは仮の作りで、残りの設計書で決める。

## 1頭の例

架空の1頭を当日に予測する場合で、モデルに何を渡して何が返ってくるかを示す。

> - **レース**: 2025年5月、東京・芝1600m・良、オープン、16頭立て
> - **馬**: 7枠14番、3歳の牝馬、斤量 55kg、馬体重 468kg（前走から +4）
> - **人**: 騎手A（前走から継続）、調教師B
> - **前走**: 3着、勝ち馬と 0.2秒差、上がり3F はこのレースの出走馬の中で 2番目に速い、35日ぶり
> - **近5走**: 平均 4.2着、最高は 1着、推定脚質は差し
> - **通算**: 8戦1勝、3着以内が 4回。東京・芝1600m は 1戦して 3着以内が 1回
> - **調教**: 直近は坂路で 4ハロン 52.8秒、ラスト1ハロン 12.3秒。14日以内に 3本
> - **血統**: 父A、母の父C
>
> → モデルの予測確率: **3着以内に入る確率 0.34**

## 使うデータ

68個の特徴量を、9つのまとまりに分けて使う（当日の場合。木曜・前日は一部を使わない。[07-prediction-timing.md](07-prediction-timing.md#時点ごとに使う特徴量) を参照）。68個の一覧は [09-features.md](09-features.md) を参照。

| まとまり | 何が分かるか | 特徴量の例 |
|---|---|---|
| A. レースの条件 | どんなレースか | 競馬場、芝ダ、距離、馬場状態、クラス、頭数 |
| B. 馬のこと | 今日の馬の条件と状態 | 馬齢、性別、枠番、斤量、馬体重とその増減 |
| C. 騎手と調教師 | 誰が乗り、誰が育てたか | 騎手、乗り替わり、騎手の近1年の3着以内の割合 |
| D. 前走 | 前のレースでどうだったか | 着順、着差、上がり3F、前走からの日数、距離の変更 |
| E. 近走のまとめ | 最近の調子と通算の成績 | 近5走の平均着順・平均着差、推定脚質、通算の3着以内の数 |
| F. この条件での経験 | 同じコース・距離・馬場で走れるか | 同じ距離帯・馬場状態での通算の3着以内の数、持ち時計の順位 |
| G. 同じレースの馬との比較 | このメンバーの中で強いか | 近5走の平均着差のレース内順位 |
| H. 血統 | どんな血か | 父、父の父、母の父 |
| I. 調教 | 仕上がり | 坂路・ウッドの直近の4ハロンとラスト1ハロン |

使わないデータとその理由は、[09-features.md](09-features.md) の「使わない特徴量」と [11-leak-prevention.md](11-leak-prevention.md) を参照。

## 流れ

LightGBM と CatBoost でどう学習・予測するかは [03-library-basics.md](03-library-basics.md)、どのクラスが何をするかは [04-classes.md](04-classes.md)、学習から予測までのやりとりは [05-sequence.md](05-sequence.md)、条件で分かれる判断の手順は [06-flowchart.md](06-flowchart.md) を参照。

## 図の使い分け

この設計書では、図を2種類だけ使い、次の方針で使い分ける。

| 図 | 表すこと | 図の1つの部品が、コードの何に当たるか | 例 |
|---|---|---|---|
| シーケンス図 | 利用者・jvdata-store・元DB と、この AI のクラスの間で、どのメソッドを、どの順に呼び、何を渡して何が返るか | 実線の矢印1本 = 呼び出し1回（クラスなら public メソッドを1回、元DB なら SQL を1回、ライブラリのモデルなら `fit` か `predict_proba` を1回）。点線の矢印1本 = その戻り値 | `PredictionWorkflow.run()` が `DatasetBuilder.build_prediction_data()` を呼び、予測用データを受け取る |
| フローチャート | 1つの public メソッドの中で、if 文によってどちらに進むか | 図1枚 = public メソッド1つの中。ひし形1つ = if 文1つ（引数のチェックのような、どのメソッドにもある if 文は描かない） | `RunnerSelector.training_samples()` が、事実表の1行ごとに「障害レースか」「出走したか」「学習データの始まり以降か」で、学習データに入れるかを決める |

public メソッドの意味は [02-glossary.md](02-glossary.md) を、クラスの一覧は [04-classes.md](04-classes.md) を参照。

- **迷ったときの決め方:** 見せたいのが「どのクラスがどのメソッドを呼び、何を渡すか」ならシーケンス図にする。「メソッドの中の if 文で、どちらに進むか」ならフローチャートにする。
- **同じ手順を両方の図で描かない。** シーケンス図の中で if 文の判断が要るところは、自分に戻る矢印に「（〇〇のフローチャート）」と書いて参照する。
- **分かれ道の無い一続きの作業は、フローチャートにしない。** シーケンス図の、自分に戻る矢印で書く。
- **置き場所:** 両方のモデルに共通の図は、シーケンス図を [05-sequence.md](05-sequence.md)、フローチャートを [06-flowchart.md](06-flowchart.md) に置く。モデルごとの図は [12-lightgbm.md](12-lightgbm.md)・[13-catboost.md](13-catboost.md) に置く。
- **見方:** 図は Mermaid で書いてある。GitHub ではそのまま図になる。VS Code のプレビューで見るには、拡張機能「Markdown Preview Mermaid Support」（`bierner.markdown-mermaid`）が要る。

## 設計書の一覧

| ファイル | 何が書いてあるか | 状態 |
|---|---|---|
| 01-overview.md | この文書。どんなデータで何を予想するか、図の使い分け | 確認待ち |
| [02-glossary.md](02-glossary.md) | 用語の意味 | 確認待ち |
| [03-library-basics.md](03-library-basics.md) | LightGBM と CatBoost の使い方（学習・予測・保存のコード例と、この予想で使う機能）。表を扱う pandas の使い方も | 確認待ち |
| [04-classes.md](04-classes.md) | どの仕事をどのクラスにやらせるか、どのクラスが流れを進めるか、クラスを置くパッケージ構成 | 確認待ち |
| [05-sequence.md](05-sequence.md) | 学習と予測のやりとり（シーケンス図） | 確認待ち |
| [06-flowchart.md](06-flowchart.md) | 学習データに入れる行の選び方（フローチャート） | 確認待ち |
| [07-prediction-timing.md](07-prediction-timing.md) | いつ予測するか（3つの時点と、時点ごとに使う特徴量） | 確認待ち |
| [08-training-data.md](08-training-data.md) | 学習データの形と、どのレース・期間を使うか | 確認待ち |
| [09-features.md](09-features.md) | 特徴量 68個の一覧と例、使わない特徴量 | 確認待ち |
| [10-target.md](10-target.md) | 目的変数の作り方 | 確認待ち |
| [11-leak-prevention.md](11-leak-prevention.md) | リークを防ぐ決まり | 確認待ち |
| [12-lightgbm.md](12-lightgbm.md) | LightGBM で学習・予測するための設計（学習データの渡し方、ハイパーパラメータの初期値、学習・予測・保存の手順） | 確認待ち |
| [13-catboost.md](13-catboost.md) | CatBoost で学習・予測するための設計（同上） | 確認待ち |
| [14-hyperparameter-settings.md](14-hyperparameter-settings.md) | 木の数などのハイパーパラメータを、設定ファイルで外から指定する方法 | 確認待ち |
| [15-decisions.md](15-decisions.md) | 決めたこと（6つ）。決めた内容と、選ばなかった選び方、関連する資料へのリンク | 決定 |
| （次に書く） | 学習データ・検証データ・テストデータの期間の分け方、評価指標、ハイパーパラメータの調整のやり方 | 未着手 |
| （次に書く） | アンサンブルのやり方、学習と予測のコマンドの引数 | 未着手 |

## 決めたこと

決めてほしいことは、すべて決まった（2026-09-21）。決めた内容と、選ばなかった選び方は [15-decisions.md](15-decisions.md) を参照。

## 文書情報

| 項目 | 内容 |
|---|---|
| 作成日 | 2026-09-21 |
| 例の値 | すべて架空。JV-Data の実際の値は載せていない |
