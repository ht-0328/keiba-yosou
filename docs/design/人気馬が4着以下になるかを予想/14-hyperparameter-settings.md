# 14 ハイパーパラメータを外から指定する

**この文書で決めること:** 木の数などのハイパーパラメータを、プログラムを書き換えずに外から指定する方法。

**結論: 設定ファイル（TOML 形式）の形も、指定のしかたの決まりも、手本とまったく同じである。この予想で違うのは、初期値のファイルの置き場所と、時点の数だけである。**

- 設定ファイルの中身の例と、表（`[ ]` の部分）の説明は [手本の 14 の「設定ファイルの形」](../近走と適性から3着以内を予想/14-hyperparameter-settings.md#設定ファイルの形) を参照。
- `--config` での指定、書き間違いをエラーにする、目的関数は変えられない、使った設定をモデルと一緒に保存する、の決まりは [手本の 14 の「決まり」](../近走と適性から3着以内を予想/14-hyperparameter-settings.md#決まり) を参照。
- 用語の意味は [02-glossary.md](02-glossary.md) を参照。

## この予想で違う点

| 項目 | 手本 | この予想 |
|---|---|---|
| 初期値の設定ファイルの置き場所 | `src/yosou/form_aptitude_top3/setting/default_settings.toml` | `src/yosou/favorites_out_of_top3/setting/default_settings.toml` |
| 同じ設定で学習するモデルの数 | 3つの時点（木曜・前日・当日） | **2つの時点（前日・当日）**（[07-prediction-timing.md](07-prediction-timing.md)） |
| 設定ファイルを読むクラス | `form_aptitude_top3/setting/` | 共通の `src/yosou/shared/setting/`（[04-classes.md](04-classes.md#1-共通の部品とこの予想だけの部品の分け方)） |
| `min_category_count` の初期値 | 2000 | 2000 のまま。行数が少ないので 600・200 と比べたが、2000 がいちばん良かった（[12-lightgbm.md](12-lightgbm.md#行数が減ることへの注意)） |

設定ファイルの中身は、初期値も項目の名前も手本と同じである。2つの予想で違う値を使いたくなったときは、それぞれの初期値のファイルを書き換える。設定ファイルを読む部品（ファイルを読む・名前を確かめる・初期値に重ねる）は共通なので、書き方の決まりは1か所で守られる。

## 文書情報

| 項目 | 内容 |
|---|---|
| 作成日 | 2026-09-22 |
| 確かめた版 | 手本と同じ（Python 3.12、lightgbm 4.7.0、catboost 1.2.10）。この予想で設定ファイルの形を変えていないので、確かめ直していない |
